/**
 * Message Tool Action Specifications (DEPRECATED)
 *
 * ⚠️ DEPRECATED (v3.6.5) - This tool is deprecated and will be removed in future versions.
 *
 * The messaging system was unused and has been removed. The t_agent_messages table no longer exists.
 * All actions will return deprecation warnings and are non-operational.
 *
 * This file is retained for backward compatibility and historical reference only.
 */

import { ActionSpec } from './types.js';

export const MESSAGE_ACTION_SPECS: Record<string, ActionSpec> = {
  // Messaging system removed in v3.6.5
  // No action specs defined - all actions are deprecated
};
