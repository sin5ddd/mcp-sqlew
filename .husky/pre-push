#!/usr/bin/env sh
echo "🚀 Running minimal pre-push smoke tests..."
echo "💡 Full testing already done in pre-commit"

# ============================================================================
# Minimal Smoke Test: Just verify build still works
# ============================================================================
echo "📦 Verifying build..."
npm run build || {
  echo "❌ Build verification failed!"
  echo "💡 This should not happen if pre-commit passed."
  echo "   Something may have changed since last commit."
  exit 1
}

echo "✅ Pre-push smoke test passed!"
echo "🎉 Ready to push!"
